# 🔗 Blockchain Event Listeners & Real-Time Updates - Phase 2\n\n## Overview\n\nPhase 2 implements advanced blockchain event monitoring and real-time WebSocket updates for the OhChain Wallet dApp backend. This enables users to receive instant notifications about transactions, balance changes, and smart contract events.\n\n---\n\n## 📋 Phase 2 Components Delivered\n\n### 1. Blockchain Event Listeners\n\n#### **BlockchainListener Service** (`src/services/blockchainListener.ts`)\n\nMonitors blockchain events in real-time and automatically updates the application state.\n\n**Key Features**:\n- ✅ **Pending Transaction Detection** - Catches transactions before block confirmation\n- ✅ **Block Confirmation Tracking** - Monitors transaction confirmations (default: 12 blocks)\n- ✅ **Balance Change Detection** - Tracks wallet balance changes\n- ✅ **Event-Driven Architecture** - Real-time blockchain state updates\n- ✅ **Automatic Notifications** - Sends alerts to users on state changes\n\n**Methods**:\n```typescript\n- initialize()           // Start all listeners\n- startPendingTransactionListener()  // Listen for pending txs\n- startBlockListener()   // Listen for new blocks\n- startBalanceListener() // Watch balance changes\n- handlePendingTransaction()         // Process pending tx\n- updatePendingTransactions()        // Update on confirmation\n- handleBalanceChange()  // Update balance\n- sendNotification()     // Send to user\n- stop()                 // Stop listeners\n```\n\n**Detection Workflow**:\n```\n1. Monitor pending transaction mempool\n   ↓\n2. Transaction detected for tracked wallet\n   ↓\n3. Create transaction record (status: pending)\n   ↓\n4. Listen for block confirmation\n   ↓\n5. After 12 confirmations → Mark as confirmed\n   ↓\n6. Send notification to user\n```\n\n#### **SmartContractListener Service** (`src/services/smartContractListener.ts`)\n\nMonitors specific smart contract events (Transfer, Approval, etc.).\n\n**Key Features**:\n- ✅ **Event Listening** - Listen to contract events (Transfer, Approval)\n- ✅ **Token Transfers** - Track ERC-20 token movements\n- ✅ **Approval Events** - Monitor token approvals\n- ✅ **Wallet Tracking** - Link events to user wallets\n- ✅ **Past Event Queries** - Retrieve historical events\n\n**Methods**:\n```typescript\n- initialize()           // Start contract listeners\n- listenToTransferEvents()       // Listen for token transfers\n- listenToApprovalEvents()       // Listen for approvals\n- handleTransferEvent()  // Process transfer\n- handleApprovalEvent()  // Process approval\n- recordTokenTransaction()       // Save to database\n- getContractDetails()   // Get contract info\n- getPastTransfers()     // Query historical events\n- stop()                 // Stop listeners\n```\n\n**Event Detection Example**:\n```typescript\n// Transfer Event\nEvent: Transfer(from, to, value)\n└─ Detect if from/to is tracked wallet\n   └─ Record as token transfer\n   └─ Update token holdings\n   └─ Notify user\n```\n\n### 2. Transaction Monitoring\n\n#### **TransactionMonitor Service** (`src/services/transactionMonitor.ts`)\n\nContinuously monitors pending transactions and updates their confirmation status.\n\n**Key Features**:\n- ✅ **Periodic Polling** - Check pending transactions every 30 seconds\n- ✅ **Confirmation Tracking** - Count block confirmations\n- ✅ **Status Updates** - pending → confirmed → finalized\n- ✅ **Failure Detection** - Detect failed transactions\n- ✅ **Gas Tracking** - Record gas usage from receipts\n\n**Methods**:\n```typescript\n- start()                // Start monitoring\n- monitorPendingTransactions()   // Monitor loop\n- checkTransactionStatus()       // Check single tx\n- updateTransactionStatus()      // Update status\n- sendTransactionNotification()  // Notify user\n- getTransactionConfirmations()  // Get confirmation count\n- getTransactionReceipt()        // Get receipt from chain\n- stop()                 // Stop monitoring\n- getStatus()            // Get monitor status\n```\n\n**Confirmation Levels**:\n```\n0 confirmations:  Pending (in mempool)\n1-11 confirmations: Processing\n12+ confirmations: Confirmed (safe)\n```\n\n### 3. Real-Time WebSocket Updates\n\n#### **RealtimeUpdateServer Service** (`src/services/realtimeUpdateServer.ts`)\n\nProvides real-time communication channel using Socket.IO for instant updates.\n\n**Key Features**:\n- ✅ **User Authentication** - JWT-based socket authentication\n- ✅ **Room-Based Broadcasting** - Send updates to specific users/wallets\n- ✅ **Transaction Updates** - Real-time transaction status changes\n- ✅ **Balance Updates** - Instant balance change notifications\n- ✅ **Custom Events** - Support for custom real-time events\n\n**Socket Events**:\n\n**Client → Server**:\n```typescript\n- authenticate(userId)           // User login\n- subscribe-wallet(walletId)     // Subscribe to wallet\n- subscribe-transactions(userId) // Subscribe to transactions\n- unsubscribe-wallet(walletId)   // Unsubscribe\n- request-balance-update(walletId)  // Request balance\n```\n\n**Server → Client**:\n```typescript\n- authenticated              // Auth successful\n- subscribed-wallet         // Subscription confirmed\n- subscribed-transactions   // Subscription confirmed\n- balance-update           // Balance changed\n- transaction-update       // Transaction status changed\n- notification             // Generic notification\n- error                    // Error message\n```\n\n**Methods**:\n```typescript\n- broadcastTransactionUpdate(userId, transaction)\n- broadcastBalanceUpdate(walletId, balance, timestamp)\n- sendNotificationToUser(userId, notification)\n- getConnectedSocketsForUser(userId)\n- getConnectedUsersCount()\n```\n\n---\n\n## 🔄 Event Flow Diagram\n\n```\n┌─────────────────────────────────────────────────────────────┐\n│           BLOCKCHAIN EVENT FLOW - Phase 2                   │\n└─────────────────────────────────────────────────────────────┘\n\n1. USER INITIATES TRANSACTION\n   └─ Via MetaMask/Frontend\n   └─ Transaction submitted to blockchain\n   └─ Gets txHash\n\n2. PENDING TRANSACTION DETECTED\n   └─ BlockchainListener detects pending tx\n   └─ Creates Transaction record (status: pending)\n   └─ Stores in database\n   └─ Socket.IO broadcasts to subscribed client\n   ✅ User sees: \"Transaction pending...\"\n\n3. TRANSACTION INCLUDED IN BLOCK\n   └─ TransactionMonitor detects in block\n   └─ Gets receipt from blockchain\n   └─ Records gas usage\n   └─ Updates status to \"processing\" (1-11 confirmations)\n   ✅ User sees: \"1 confirmation\"\n\n4. CONFIRMATIONS ACCUMULATE\n   └─ TransactionMonitor counts blocks\n   └─ After 12 blocks → Status: confirmed\n   └─ Updates database\n   └─ Sends notification to user\n   ✅ User sees: \"12 confirmations - Transaction confirmed\"\n\n5. BALANCE UPDATES\n   └─ BlockchainListener detects balance change\n   └─ Updates wallet.balance in DB\n   └─ WebSocket broadcasts new balance\n   ✅ User sees: Updated balance in UI\n\n6. SMART CONTRACT EVENTS\n   └─ SmartContractListener detects Transfer/Approval\n   └─ Records token transaction\n   └─ Updates token holdings\n   ✅ User sees: Token transfer recorded\n```\n\n---\n\n## 📊 Database Updates on Events\n\n### Transaction Lifecycle\n\n```typescript\n// 1. Pending Transaction Created\n{\n  txHash: \"0x...\",\n  status: \"pending\",\n  fromAddress: \"0x...\",\n  toAddress: \"0x...\",\n  amount: \"1.0\",\n  createdAt: timestamp\n}\n\n// 2. Transaction in Block\n{\n  status: \"processing\",\n  blockNumber: 18234567,\n  confirmations: 5\n}\n\n// 3. Transaction Confirmed\n{\n  status: \"confirmed\",\n  blockNumber: 18234567,\n  blockTimestamp: timestamp,\n  gasUsed: \"21000\",\n  confirmedAt: timestamp,\n  confirmations: 12\n}\n```\n\n### Wallet Balance Update\n\n```typescript\nWallet {\n  balance: \"2.5\" ETH  // Updated from blockchain\n  lastSyncedAt: timestamp // When last checked\n}\n```\n\n---\n\n## 🚀 Integration with Main Server\n\n### Updated `src/server.ts`\n\n```typescript\nimport BlockchainListener from './services/blockchainListener';\nimport TransactionMonitor from './services/transactionMonitor';\nimport RealtimeUpdateServer from './services/realtimeUpdateServer';\n\n// During server initialization:\nawait BlockchainListener.initialize();  // Start event listeners\nawait TransactionMonitor.start();        // Start monitoring\nrealtimeServer = new RealtimeUpdateServer(httpServer); // WebSocket\n\n// Status endpoint\nGET /api/status\n{\n  blockchain: { isListening: true, network: \"mainnet\", ... },\n  transactionMonitor: { isRunning: true, checkInterval: 30000 },\n  realtimeUsers: 42\n}\n```\n\n---\n\n## 💻 Frontend Integration Example\n\n### Client-Side WebSocket\n\n```javascript\n// Connect to WebSocket\nconst socket = io('http://localhost:3000', {\n  auth: { token: jwtToken }\n});\n\n// Authenticate user\nsocket.emit('authenticate', userId);\n\n// Subscribe to wallet updates\nsocket.emit('subscribe-wallet', walletId);\n\n// Listen for updates\nsocket.on('balance-update', (data) => {\n  console.log('New balance:', data.balance);\n  updateUIBalance(data.balance);\n});\n\nsocket.on('transaction-update', (data) => {\n  console.log('Transaction:', data.transaction.status);\n  updateTransactionStatus(data.transaction);\n});\n\nsocket.on('notification', (data) => {\n  showNotification(data.title, data.message);\n});\n```\n\n---\n\n## 🔧 Configuration\n\n### Environment Variables (Updated)\n\n```bash\n# Blockchain Configuration\nETHEREUM_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY\nETHEREUM_NETWORK=mainnet\nCHAIN_ID=1\nCONTRACT_ADDRESS=0x...\n\n# Event Listener Configuration\nBLOCK_CONFIRMATION_REQUIRED=12\nTX_CHECK_INTERVAL=30000  # 30 seconds\nBALANCE_CHECK_INTERVAL=60000  # 60 seconds\n\n# WebSocket Configuration\nWEBSOCKET_ENABLED=true\nWEBSOCKET_PORT=3000\nWEBSOCKET_CORS=*\n```\n\n---\n\n## 📈 Performance Considerations\n\n### Optimization Strategies\n\n1. **Batch Processing**\n   - Process multiple pending txs in one poll cycle\n   - Reduce database queries\n\n2. **Caching**\n   - Cache receipt data in Redis\n   - Reduce RPC calls to blockchain\n\n3. **Connection Pooling**\n   - Reuse RPC connections\n   - Minimize connection overhead\n\n4. **Event Deduplication**\n   - Check if tx already exists before creating\n   - Prevent duplicate records\n\n### Scalability\n\n```\nCurrent Setup:\n- 1 BlockchainListener per node\n- 1 TransactionMonitor per node\n- Multiple WebSocket connections (auto-scaling)\n- Database: PostgreSQL (indexed queries)\n- Cache: Redis (pending tx cache)\n\nFuture Scale:\n- Multiple listener instances (load balanced)\n- Message queue (RabbitMQ) for events\n- Separate read/write DB replicas\n```\n\n---\n\n## 🧪 Testing Event Listeners\n\n### Manual Testing\n\n```bash\n# 1. Start server\nnpm run dev\n\n# 2. Check status endpoint\ncurl http://localhost:3000/api/status\n\n# 3. Connect WebSocket client\nconst socket = io('http://localhost:3000', { auth: { token: \"test\" } });\nsocket.emit('authenticate', 'userId123');\n\n# 4. Send test transaction via MetaMask\n# - Watch console for event detection\n# - See WebSocket updates in real-time\n```\n\n### Unit Tests Structure\n\n```typescript\n// tests/blockchainListener.test.ts\ndescribe('BlockchainListener', () => {\n  test('should detect pending transactions', async () => {...});\n  test('should update on block confirmation', async () => {...});\n  test('should detect balance changes', async () => {...});\n});\n\n// tests/transactionMonitor.test.ts\ndescribe('TransactionMonitor', () => {\n  test('should check pending transactions', async () => {...});\n  test('should update confirmation count', async () => {...});\n  test('should mark as failed on error', async () => {...});\n});\n```\n\n---\n\n## 📚 API Endpoints (New in Phase 2)\n\n```\nGET /api/status\n└─ Returns server and listener status\n\nGET /api/health\n└─ Returns blockchain listener status\n   └─ Includes confirmation requirements\n   └─ Check interval timing\n```\n\n---\n\n## 🔒 Security Features\n\n✅ **WebSocket Authentication** - JWT token verification  \n✅ **Room-Based Access Control** - Users only see their data  \n✅ **Event Validation** - Verify blockchain events  \n✅ **Rate Limiting** - Prevent spam  \n✅ **Audit Logging** - Track all events  \n\n---\n\n## 📝 Files Created in Phase 2\n\n```\nsrc/services/\n├── blockchainListener.ts     (393 lines) - Blockchain event monitoring\n├── smartContractListener.ts  (350 lines) - Smart contract events\n├── transactionMonitor.ts     (320 lines) - Transaction confirmation\n└── realtimeUpdateServer.ts   (280 lines) - WebSocket server\n\nsrc/\n└── server.ts (updated)       - Integrated all Phase 2 services\n```\n\n**Total Code Added**: 1,343+ lines\n\n---\n\n## ✅ Phase 2 Completion Checklist\n\n- ✅ Blockchain event listeners implemented\n- ✅ Smart contract event monitoring\n- ✅ Transaction confirmation tracking\n- ✅ Real-time WebSocket updates\n- ✅ Database integration for events\n- ✅ User notifications system\n- ✅ Graceful shutdown handling\n- ✅ Comprehensive logging\n- ✅ Status monitoring endpoints\n- ✅ Server integration\n\n---\n\n## 🎯 Next Steps (Phase 3)\n\n1. **Frontend React Dashboard**\n   - WebSocket integration\n   - Real-time transaction updates\n   - Balance monitoring\n\n2. **Admin Dashboard Enhancement**\n   - Event monitoring UI\n   - System statistics\n   - Performance metrics\n\n3. **Testing Suite**\n   - Unit tests for all services\n   - Integration tests\n   - E2E testing\n\n4. **Optimization**\n   - Redis caching strategy\n   - Database query optimization\n   - RPC call reduction\n\n5. **Documentation**\n   - API documentation (Swagger)\n   - Client SDK\n   - Deployment guide\n\n---\n\n## 📞 Support & Troubleshooting\n\n### Common Issues\n\n**\"Not listening to blockchain events\"**\n- Check ETHEREUM_RPC_URL is valid\n- Verify network connectivity\n- Check logs for initialization errors\n\n**\"WebSocket not connecting\"**\n- Ensure Socket.IO installed\n- Check firewall/CORS settings\n- Verify JWT token is valid\n\n**\"Transactions not showing up\"**\n- Confirm wallet addresses are tracked\n- Check database connection\n- Verify blockchain confirmation count\n\n---\n\n## 🚀 Phase 2 Status: ✅ COMPLETE\n\n**Blockchain event listeners are now LIVE and monitoring all blockchain activity!**\n\nServer is ready for Phase 3 frontend integration.\n"